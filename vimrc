set tabstop=4		" number of space chars inserted when tab is pressed
set shiftwidth=4	" number of space chars inserted for indentation
set expandtab		" insert space instead of tab
set autoindent
set background=dark

cmap w!! w !sudo tee %
